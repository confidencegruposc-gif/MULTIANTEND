{
  "name": "multiatend",
  "version": "1.0.0",
  "description": "WhatsApp Kanban com IA",
  "main": "backend/server.js",
  "scripts": {
    "install-all": "npm install --prefix backend && npm install --prefix frontend",
    "build": "npm run build --prefix frontend",
    "start": "npm install --prefix backend && npm run build --prefix frontend && node backend/server.js"
  },
  "engines": {
    "node": "18.x"
  }
}
